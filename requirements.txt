# Core PDF / document processing
PyMuPDF>=1.24.0

# Configuration
python-dotenv>=1.0.0

# Primary OCR: PaddleOCR-VL
paddleocr>=2.9.0
paddlepaddle>=2.6.0

# Unlimited-OCR fallback + Qwen3-VL supervisor
torch>=2.2.0
transformers>=4.44.0
huggingface_hub>=0.24.0
accelerate>=0.33.0

# Required by baidu/Unlimited-OCR's remote modeling code
# (transformers trust_remote_code pulls these in at load time)
addict>=2.4.0
matplotlib>=3.8.0

# Numeric support used across OCR/extraction backends
numpy>=1.26.0
Pillow>=10.0.0